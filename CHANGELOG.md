# 更新日志

## v0.5 UI Preview

- 接入 DS4 / XInput / DualSense 三套手柄底图资源。
- 手柄预览改为底图加透明热区，不再使用粗糙的独立按钮覆盖。
- 新增热区校准模式：可拖动热区位置，右键恢复默认。
- 默认只在悬停、按下、校准时显示高亮反馈，降低视觉干扰。
- 保留手柄检测、映射、保存配置、加载配置、轮询率选择。
- README 移除作者署名，软件界面内保留署名。

## v0.4

- 取消中间映射表，改为点击手柄按键直接设置映射。
- 映射目标弹窗增加图形化键盘/鼠标选择。
- 选择目标后保留高亮提示，点击完成后保存。
- XInput 模式不再显示 DS 专属按键。
- DS4 模式实时按键状态使用 DS 风格名称。

## 第二版重构

- 支持 DS4 / HID / XInput 检测。
- 增加防抖状态机 `ButtonDown` / `ButtonHeld` / `ButtonUp`。
- 输入线程与 UI 线程分离。
- Auto 模式优先 XInput，失败后 fallback HID。
- 修复映射编辑弹窗卡死。
- 修复 DS4 按键闪烁和重复触发。
