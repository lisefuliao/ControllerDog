# 更新日志

## v0.5 UI Preview

- 接入 DS4 / DualSense / XInput 三套手柄底图资源。
- 手柄预览改为“底图 + 透明热区”方案，减少遮挡和突兀的额外按键 UI。
- 新增热区校准模式，可手动拖动不准确的点击区域，右键恢复默认位置。
- 热区偏移保存到本机用户配置目录，下次启动自动加载。
- 保留浅色 / 深色主题与主题色切换。
- README 移除作者署名，仅在软件界面中保留署名。

## v0.4

- 取消中间映射表，改为点击手柄按键直接设置映射。
- 映射目标弹窗增加图形化键盘 / 鼠标选择。
- 选择映射目标后保留高亮提示，点击完成后保存。
- XInput 模式不再显示 DS 专属按键。
- DS4 模式实时按键状态使用 DS 风格名称。

## 第二版重构

- 支持 DS4 / HID / XInput 检测。
- 增加 `ButtonDown` / `ButtonHeld` / `ButtonUp` 防抖状态机。
- 输入线程与 UI 线程分离。
- Auto 模式优先 XInput，失败后 fallback HID。
- 修复映射编辑弹窗卡死。
- 修复 DS4 按键闪烁和重复触发。
